# This file should be at the end of the _ files (they are sorted
# alphabetically) to ensure that ~/bin is at the beginning of the path in order
# to override executables.

# include ~/bin for local executables
PATH=~/bin:$PATH
