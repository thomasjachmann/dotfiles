#!/bin/bash

# SSH with different color scheme

function reset_style {
  tmux select-pane -P $old_style
}

# This only works if we're running inside tmux
if [ -n "$TMUX" ]; then
  old_style=$(tmux select-pane -g)
  trap reset_style EXIT
  tmux select-pane -P "bg=#404042,fg=#b5b5b5"
fi

/usr/bin/ssh "$@"
