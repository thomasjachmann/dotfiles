ext.modal = {}

function ext.modal.bind()
end
--class HotkeyGroup

--  def initialize(hotkeys)
--    @hotkeys = hotkeys
--  end

--  def enable
--    @hotkeys.map(&:enable)
--  end

--  def disable
--    @hotkeys.map(&:disable)
--  end

--end

--modal = function(hotkeys)
--  group = HotkeyGroup.new(hotkeys)
--  group.disable()
--  return group
--end

--hydra.bind(hyper, space, modal({
--  hydra.hotkey(hyper, x, 
--}))

--{hyper, space, {
--  {
--}}
