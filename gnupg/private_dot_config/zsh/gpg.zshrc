# alias gpg-search-keys='gpg2 --keyserver hkp://keys.gnupg.net --search-keys'
# no need to alias this, just include the ~/.gnupg dir (only the public files), there's this keyserver set in a config file
